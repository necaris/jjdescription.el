name: Test

on:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: purcell/setup-emacs@master
        with:
          version: '30.1'
      - run: emacs --batch -L . -L test -l jjdescription-test.el -f ert-run-tests-batch-and-exit
